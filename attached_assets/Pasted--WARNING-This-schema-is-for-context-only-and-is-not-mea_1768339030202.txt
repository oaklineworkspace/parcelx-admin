-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.airlines (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  code text NOT NULL UNIQUE,
  name text NOT NULL,
  logo_url text,
  country text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT airlines_pkey PRIMARY KEY (id)
);
CREATE TABLE public.airports (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  code text NOT NULL UNIQUE,
  name text NOT NULL,
  city text NOT NULL,
  country text NOT NULL,
  country_code text,
  timezone text,
  latitude numeric,
  longitude numeric,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT airports_pkey PRIMARY KEY (id)
);
CREATE TABLE public.crypto_wallets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  crypto_name text NOT NULL,
  crypto_symbol text NOT NULL,
  network_type text NOT NULL,
  wallet_address text NOT NULL,
  is_active boolean DEFAULT true,
  display_order integer DEFAULT 0,
  icon_url text,
  min_confirmations integer DEFAULT 1,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT crypto_wallets_pkey PRIMARY KEY (id)
);
CREATE TABLE public.email_verification_codes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL,
  code_hash text NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  attempts integer DEFAULT 0,
  verified boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT email_verification_codes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.flight_bookings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  booking_reference text NOT NULL UNIQUE,
  outbound_flight_id uuid,
  return_flight_id uuid,
  departure_date date NOT NULL,
  return_date date,
  trip_type text NOT NULL DEFAULT 'roundtrip'::text,
  cabin_class text NOT NULL DEFAULT 'economy'::text,
  total_passengers integer NOT NULL DEFAULT 1,
  total_price numeric NOT NULL,
  taxes_fees numeric NOT NULL DEFAULT 0,
  status text NOT NULL DEFAULT 'pending'::text,
  payment_status text DEFAULT 'unpaid'::text,
  contact_email text,
  contact_phone text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT flight_bookings_pkey PRIMARY KEY (id),
  CONSTRAINT flight_bookings_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT flight_bookings_outbound_flight_id_fkey FOREIGN KEY (outbound_flight_id) REFERENCES public.flights(id),
  CONSTRAINT flight_bookings_return_flight_id_fkey FOREIGN KEY (return_flight_id) REFERENCES public.flights(id)
);
CREATE TABLE public.flight_passengers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  booking_id uuid NOT NULL,
  passenger_type text NOT NULL DEFAULT 'adult'::text,
  title text,
  first_name text NOT NULL,
  last_name text NOT NULL,
  date_of_birth date,
  nationality text,
  passport_number text,
  passport_expiry date,
  email text,
  phone text,
  seat_preference text,
  meal_preference text,
  special_requests text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT flight_passengers_pkey PRIMARY KEY (id),
  CONSTRAINT flight_passengers_booking_id_fkey FOREIGN KEY (booking_id) REFERENCES public.flight_bookings(id)
);
CREATE TABLE public.flights (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  flight_number text NOT NULL,
  airline_id uuid NOT NULL,
  departure_airport_id uuid NOT NULL,
  arrival_airport_id uuid NOT NULL,
  departure_time time without time zone NOT NULL,
  arrival_time time without time zone NOT NULL,
  duration_minutes integer NOT NULL,
  aircraft_type text,
  base_price_economy numeric NOT NULL DEFAULT 100,
  base_price_premium numeric NOT NULL DEFAULT 250,
  base_price_business numeric NOT NULL DEFAULT 800,
  base_price_first numeric NOT NULL DEFAULT 2500,
  stops integer DEFAULT 0,
  stop_airports ARRAY,
  days_of_week ARRAY DEFAULT '{1,2,3,4,5,6,7}'::integer[],
  is_active boolean DEFAULT true,
  amenities ARRAY,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT flights_pkey PRIMARY KEY (id),
  CONSTRAINT flights_airline_id_fkey FOREIGN KEY (airline_id) REFERENCES public.airlines(id),
  CONSTRAINT flights_departure_airport_id_fkey FOREIGN KEY (departure_airport_id) REFERENCES public.airports(id),
  CONSTRAINT flights_arrival_airport_id_fkey FOREIGN KEY (arrival_airport_id) REFERENCES public.airports(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  full_name text,
  email text UNIQUE,
  avatar_url text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  first_name text,
  last_name text,
  country text,
  contact_address text,
  country_code text,
  phone_number text,
  receive_updates boolean DEFAULT false,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.shipment_images (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  shipment_id uuid NOT NULL,
  image_url text NOT NULL,
  caption text,
  uploaded_at timestamp with time zone DEFAULT now(),
  CONSTRAINT shipment_images_pkey PRIMARY KEY (id),
  CONSTRAINT shipment_images_shipment_id_fkey FOREIGN KEY (shipment_id) REFERENCES public.shipments(id)
);
CREATE TABLE public.shipments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  tracking_number text NOT NULL UNIQUE,
  status text NOT NULL DEFAULT 'Pending'::text,
  origin text NOT NULL,
  destination text NOT NULL,
  estimated_delivery timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  sender_name text,
  sender_phone text,
  sender_email text,
  receiver_name text,
  receiver_phone text,
  receiver_email text,
  weight numeric,
  dimensions text,
  package_type text DEFAULT 'Standard'::text,
  shipping_method text DEFAULT 'Standard'::text,
  notes text,
  declared_value numeric,
  sender_address text,
  sender_city text,
  sender_state text,
  sender_postal_code text,
  sender_country text,
  receiver_address text,
  receiver_city text,
  receiver_state text,
  receiver_postal_code text,
  receiver_country text,
  contents_description text,
  insurance_value numeric,
  insurance_type text,
  service_level text DEFAULT 'standard'::text,
  special_instructions text,
  is_fragile boolean DEFAULT false,
  requires_signature boolean DEFAULT false,
  actual_delivery timestamp with time zone,
  current_location text,
  last_scan_time timestamp with time zone,
  item_name text,
  item_quantity integer DEFAULT 1,
  item_category text,
  customs_value numeric,
  customs_currency text DEFAULT 'USD'::text,
  hs_code text,
  country_of_origin text,
  is_gift boolean DEFAULT false,
  commercial_invoice_number text,
  CONSTRAINT shipments_pkey PRIMARY KEY (id),
  CONSTRAINT shipments_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.tracking_updates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  shipment_id uuid,
  location text NOT NULL,
  description text,
  status text NOT NULL,
  occurrence_time timestamp with time zone DEFAULT now(),
  event_code text,
  status_category text DEFAULT 'in_transit'::text,
  status_detail text,
  facility_name text,
  facility_type text,
  handler text,
  city text,
  state text,
  country text,
  postal_code text,
  latitude numeric,
  longitude numeric,
  scan_time timestamp with time zone DEFAULT now(),
  expected_next_event text,
  is_exception boolean DEFAULT false,
  exception_code text,
  signed_by text,
  CONSTRAINT tracking_updates_pkey PRIMARY KEY (id),
  CONSTRAINT tracking_updates_shipment_id_fkey FOREIGN KEY (shipment_id) REFERENCES public.shipments(id)
);